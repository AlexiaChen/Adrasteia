1. npm init
2. mkdir dist & mkdir src
3. cd src -> mkdir script -> mkdir style
script 文件夹存放脚本文件
style 存放css文件，当然，也可以是less 或者Sass写的

4. new file [webpack.config.js]  webpack的配置文件 http://webpack.github.io/docs/
5. webpack or npm run webpack
如果不用第二个命令行，webpack默认会读取webpack.config.js文件 